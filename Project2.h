#ifndef Project2_h
#define Project2_h

#include <stdio.h>
#include <vector>
#include <string>

class Project2 {
    public:
        void ReadFile(string inputFileName, string outputFileName);
        /*void CreateDirector(vector<double> inputVals);
        void CreateVehicle(vector<double> state, vector<double> inputVals);
    private:
        vector <string> substrings;*/
};

#endif

